import { Outlet } from "react-router-dom";
// import Nav from "../Components/Nav/Nav";

const Route = () => {
  return (
    <div className="">
      <Outlet></Outlet>
    </div>
  );
};

export default Route;
